language: node_js
node_js:
  - "6"

before_script:
  - "npm install -g gulp"

script:
  - "npm install"
  - "gulp"

deploy:
  provider: script
  script: "scripts/deploy.sh"
  on:
    branch: "stable"
